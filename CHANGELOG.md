# 変更履歴

このファイルは NextForm の版ごとの変更点を記録します。
fork 元の ToraToraWiki 1.3.10 (rev.1285) からの差分という視点で書いています。

版数は `メジャー.マイナー.パッチ` です。1.0 になるまでは、マイナーの繰り上げで
機能の区切りを、パッチで修正を表します。

---

## v0.7.0 — (未リリース)

**マニュアルが最初から入っているようになりました。**

### 変更

- **マニュアルが組み込みになりました。** これまでは管理ツールの
  「マニュアル生成」を 1 回押すと、`NextFormManual` 以下に**普通のページとして
  書き出される**仕組みでした。押すまでは存在せず、サイトメニューの
  「マニュアル」は GitHub に飛ぶだけでした。
  いまは**設置した時点で読めます**。操作は要りません
- **アップグレードするとマニュアルの中身も新しくなります。** 以前は
  書き出したページが `storage/` に残るため、NextForm を新しくしても
  マニュアルは古いままでした
- **マニュアルは編集できなくなりました。** 表示専用です。
  以前はロックを外せば書き換えられました
- **一般のページから `[[NextFormManual/ja]]` のようにリンクを張れます。**
  存在しないページ扱いにはなりません
- **検索は今までどおりマニュアルも対象にします。** 検索画面の
  「マニュアルは除く」もそのままです
- **管理ツールの「マニュアル生成」は無くなりました**

### 片付け (任意)

以前にマニュアルを生成したことがあるサイトでは、`NextFormManual` 以下
(v0.5.0 より前なら `ToraToraWikiManual` 以下) の**ページが残ります**。
表示には使われません。消したい場合は `?option=delete` から削除してください。
アップグレードツールが、残っている件数を知らせます。

---

## v0.6.0 — 2026-08-19

**Markdown で書けるようになりました。**

### 追加

- **ページ種別に `markdown` が増えました。** ページを作るときに
  `wiki` / `markdown` / `text` / `file` から選びます (種別は後から変えられません)。
  [CommonMark](https://commonmark.org/) に準拠し、表・打ち消し線・
  チェックボックス・自動リンク・脚注 (GitHub Flavored Markdown) が使えます。
  Obsidian や Notion で書いた文章をそのまま貼り付けても、おおむね同じ見え方になります
- **`[[ページ名]]` でページにリンクできます。** 書き方は Obsidian や MediaWiki と
  同じく**対象が先**です (`[[ページ名|表示名]]`)。相対指定 (`[[../隣]]`)、
  見出しへのリンク (`[[ページ名#見出し]]`) も使えます。
  存在しないページは Wiki 記法と同じ色で示され、見出しの位置の決め方も同じなので、
  種別をまたいでリンクできます
- **`![説明](ファイル名)` で添付を埋め込めます。** まず子ページとして探すので、
  「添付」でアップロードしたファイル名をそのまま書けます
- **先頭のメタ情報 (`---` で囲んだフロントマター) は本文に出ません。**
  他のツールから持ってきた文章をそのまま貼れます
- 見出しから**目次**が作られます (ページメニューの「目次」)
- 検索は Markdown ページの本文も拾います (表のセルや脚注の中身も含みます)
- **色調を自分で作って保存できるようになりました。** 管理ツールに
  **「色調の設定」**が増えました。25 色をそのまま並べてあり、
  組み込みや保存した色調を「読み込む」で入力欄に入れてから調整できます。
  名前を付けて保存すると、次からは読み込みの一覧に出ます (新しいものほど上)。
  色調 1 つがファイル 1 つ (`storage/tone/<識別子>.json`) なので、
  サーバの管理者が直接置いたり、別のサイトへ複製したりできます。
  はじめから入っている 6 色調と同じ形式で、同じ識別子で保存すれば上書きでき、
  ファイルを消せば元に戻ります (マニュアルの「色調(トーン)」を参照)
- **管理ツールや個人設定の下の画面から、親へ戻れるようになりました。** 見出しの後ろに
  「(管理ツール/サイト設定)」と道筋が出ます。一般のページと同じ形で、どちらもリンクです。
  「権限設定」は「ユーザーの管理」から入るので 3 段になります。
  「パスワード設定」は入口によって道筋が変わります (自分の分は個人設定の下、
  他人の分はユーザーの管理の下、パスワード再発行のリンクから開いたときは出ません)

> **生の HTML は、既定では HTML として解釈されません** (そのまま文字として出ます)。
> 編集できる人が誰でもスクリプトを埋め込める状態を避けるためです。
> サイト設定の `MARKDOWN_ALLOW_HTML` で解釈させることもできます。

### 変わったところ

- **色の設定が「外観の設定」から「色調の設定」に移りました。** 配置や文字の
  大きさと色が同じ画面に混在して分かりにくかったためです。「外観の設定」は
  レイアウト・文字・ロゴ・見出しの構成・背景画像だけになりました。
  色調を 1 つ選ぶ段は無くなり、**最初から 25 色を直接扱います**

### 修正

- **検索したときにサーバのログへ警告が出ることがあったのを直しました。**
  消したページが検索インデックスに残っていると、検索のたびに警告が積まれていました
- **「マニュアル生成」と「他 wiki からのインポート」の実行ボタンが効かなかったのを
  直しました。** v0.4.0 で入れた CSRF 対策に弾かれ、押しても何も起きない状態でした
- **「検索インデックスの再構築」が実行を押した直後にエラーになっていたのを
  直しました。** 時間のかかる処理は途中で画面を返して続きを呼び直す作りですが、
  その呼び直しが GET だったため、これも v0.4.0 の CSRF 対策に弾かれていました。
  続きをフォームで送るようにしたので、同じ作りの「他 wiki からのインポート」も
  最後まで走ります (索引そのものは無事でした。作り終えるまで差し替えないためです)
- **画像を縮小して表示するたびに、サーバのログへ非推奨の警告が出ていたのを
  直しました。** 縮小は wiki の中のファイルをストリームとして開く経路を通ります。
  そこで使うクラスがプロパティを 1 つ宣言していませんでした
- **色の設定がいつも同じ色から始まっていたのを直しました。**
  いま使っている色調の色が入力欄に入るので、見た目を変えずに調整を始められます
- **入力欄に枠が付きました。** 背景色と「入力時の背景色」が近い色調では、
  入力欄がどこにあるのか分からない状態でした
- **差し替えたロゴ画像・favicon を標準に戻せるようになりました。**
  これまでの「削除」は「表示しない」の意味で、一度差し替えるとテーマ同梱の
  画像に戻す手段がありませんでした。「標準に戻す」と「表示しない」から選べます
  (見出しや本文の背景画像は同梱の画像を持たないので「表示しない」だけです)。
  画像を選ぶ操作と同時に指定した場合は、どちらも行わずにその場で知らせます。
  「サイトのお気に入りアイコン」は **(favicon)** と補いました

---

## v0.5.0 — 2026-08-19

**見た目を選べるようになりました。**

### 追加

- **テーマが 4 つになりました。** 管理ツール → サイト設定 → テーマ で選べます
  - `basic` … これまでどおり
  - `plain` … 塗りをやめ、細い線と広い行間で読ませる
  - `docs` … 本文の幅を止め、見出しの深さで階層を示し、サイドを画面に留める
  - `card` … 本文とサイドを角丸のカードに載せる
  - **色調 (6 種 + 個別指定) の設定は、テーマを変えてもそのまま引き継がれます**
- **本文の詰まり具合を選べます** (管理ツール → 外観の設定 → 本文の詰まり具合)。
  「ゆったり / 標準 / 詰める」で、余白・行間・表のセルがまとめて変わります
- **整形済みテキストの長い行**を、折り返すか横スクロールにするか選べます。
  ブロックごとに `&pre(wrap)` / `&pre(nowrap)` でも指定できます
  (アスキーアート `&pre(paa)` は従来どおり折り返しません)

### 変更

- **ロゴが NextForm 自身のものになりました。** 上流 ToraToraWiki の猫の画像を
  差し替えています。フッターの表記も `Powered by NextForm` になりました
- **組み込みマニュアルのページ名が `ToraToraWikiManual` から `NextFormManual` に
  変わりました。** 既存のページは残ります。管理ツール →「マニュアル生成」を
  1 回実行すると新しい名前で作り直されます
- ブラウザに保存する cookie の名前が `toratorawiki_` から `nextform_` に変わりました
  (サイドメニューの開閉状態が 1 度だけ忘れられます)

---

## v0.4.1 — 2026-08-18

**v0.4.0 の監査で残した項目を片付けた版。** 動作は変わりません。

### 修正

- **`&link()` が値の一部だけ URL に一致しても URL として扱っていた。**
  `&link(abchttp://example.com/);` が `href="abchttp://example.com/"` に
  なっていました。値の全体が URL のときだけリンクにします。通るスキームは
  `http` / `https` / `ftp` / `file` だけです。
  本文中の URL の自動リンクと `[[表示>URL]]` は変わりません
- **新しく設置するサイトは、サイトごとに違う認証 realm を使うようになった。**
  保存されるのは `md5(利用者名:realm:パスワード)` で、realm が全サイト共通の
  `'User login'` だったため、**同じ事前計算表がどの NextForm サイトにも
  通用していました**

> **既存サイトの realm は変わりません。**変えると保存済みのダイジェストが
> すべて無効になり、全利用者がログインできなくなります (認証時に平文は
> 届かないので、サーバー側で作り直すことはできません)。
> アップグレードしても今までどおり使えます。

### 追加

- **`?option=admin_info` に「設置」を追加した。** `index.php` `app/`
  `app/plugin/` を Web サーバーが書き換えられる状態かを表示します。
  書ける場合、ファイルを 1 つ書ける不具合が見つかれば任意のコードを
  実行されます (`app/plugin/*.inc` は起動のたびに読み込まれます)。
  **コードでは塞げない**ため表示だけにしてあります。分け方は
  `docs/installation.md` の「コードを Web サーバーに書かせない」を参照してください

### 開発

- `tests/setup-fixtures.sh` が `tests/env.local` を読むようにした。
  他のテストは全部読んでいるのにここだけ読んでおらず、管理者名が `admin`
  以外のサイトでは認証に失敗して 1 件も書けませんでした。しかも投入前に
  既存ページを消すため、フィクスチャが消えたままゴールデンマスターが
  3 件とも落ちる状態になっていました

## v0.4.0 — 2026-08-18

**CSRF 対策を入れ、検索を速くした版。**
罠サイトを開いただけでページが消える経路を塞ぎ、検索の待ち時間を 1/10 前後にしました。

> **この版から、状態を変える操作は POST でしか受け付けません。**
> ブラウザからの操作は今までどおりです (フォームは元から POST でした)。
> 影響があるのは、`?ページ名&option=delete&action=write` のような URL を
> スクリプトやブックマークから叩いている場合だけです。
> `docs/upgrade-guide.md` (配布物では `UPGRADE.md`) を参照してください。

### 修正 — セキュリティ

- **CSRF 対策を入れた。** 上流には Referer / Origin / SameSite の検査が
  1 つも無く、さらに `args_get()` が `$_GET + $_POST` だったため、
  **GET だけでページを作ったり消したりできました**。HTTP ダイジェスト認証は
  ブラウザが資格情報を自動で付けるので、`<img src="…?ページ名&option=delete&action=write">`
  を 1 つ貼れば、それを見たログイン中の利用者のページが消えました。
  実サイトの複製で、作成・削除とも成立することを確認しています。
  状態を変える操作を POST に限り、同一オリジンからのものだけ通すようにしました
- **ダイジェスト認証の nonce を検証するようにした。** サーバーが自分の発行した
  nonce かどうかを一度も確かめておらず、再送カウンタ (`nc`) も期限もありませんでした。
  `Authorization` ヘッダを 1 度捕まえれば無期限に再利用できました。
  併せて、応答の突き合わせを定数時間 (`hash_equals`) に、nonce の生成を
  `uniqid()` から暗号論的乱数に変えました

> XSS・パストラバーサル・ファイルアップロード・コマンドインジェクションも
> 調べましたが、**問題は見つかりませんでした**。調査結果は
> `docs/development/project-overview.md` に残してあります。

### 修正 — 安定性

- **存在しない action を指定しても 500 にならなくなった。** `handle()` と `option()` が
  登録表のキーを全部「URL から呼べる action」として扱っていました。登録表には
  画面を出す action と内部から使う部品 (`texts` / `normalize`) が混ざっており、
  後者を URL から呼ぶと引数が合わずに落ちます。さらに上流には**実体の無い登録が
  3 件**残っていました (`listedit` の `insert`、`history` の `diff`、および
  `wiki` の `normalize` の呼び違い)。呼ぶ前に実体を確かめるようにしたので、
  同じ書き損じが増えても 500 にはなりません

### 変更 — 性能

- **検索の本文抽出をページごとにキャッシュするようにした。** 検索は候補ページごとに
  本文をフルパース (字句解析 → 構文解析 → DOM 構築) し直していました。
  1372 ページの実サイトで計ると、**検索時間の 92〜95% がここ**で、索引の引き込みは
  0.1〜2.9ms しかありませんでした。遅い原因は索引ではなく本文の再パースでした。

  ブラウザから見た応答時間 (1372 ページ、認証あり):

  | 検索語 | 件数 | 変更前 | 変更後 |
  |---|---|---|---|
  | 設定 | 256 | 0.56 秒 | **0.05 秒** |
  | の | 564 | 0.48 秒 | **0.08 秒** |

  1 回目は少し遅くなります (キャッシュを作るため。実測で約 17%)。2 回目から上の速さです。
  `storage/cache/` が 1 ページあたり約 2.5KB 増えます (1372 ページで約 3.4MB)。

> **キャッシュが外れても検索結果は変わりません。** 見つからない・読めない・古い、
> のどれでも作り直すだけです。有効かどうかはページの更新時刻だけで判断するので、
> 編集や削除は次の検索にそのまま反映されます。`storage/cache/` を消しても
> 自動で作り直されます (**検索インデックスとは違います**)。

### 追加

- `CSRF_PROTECTION` (既定 `true`)。ブラウザ以外から POST している運用のための逃げ道です。
  **切ると罠サイトからページを消せる状態に戻ります**
- `tests/csrf.sh` — CSRF 対策と nonce 検証のテスト (33 項目)
- `tests/dispatch.sh` — 不正な action で 500 にならないかのテスト (21 項目)
- `tests/search-cache.sh` — テキストキャッシュのテスト (24 項目)

## v0.3.1 — 2026-08-17

**`storage/` のディレクトリ一覧が外から見えるのを塞いだ版。**

### 修正

- 配布する `.htaccess` に `Options -Indexes` を追加した。
  個別のファイルは以前から拒否されていましたが、**ディレクトリの一覧は見えており**、
  `storage/page/` にアクセスするとページ名 (16 進) が並んでいました。
  どんなページが存在するかが外から分かる状態でした

> **既存サイトでは手で 1 行足してください。** `.htaccess` は利用者が独自に
> 手を入れていることがあるため、アップグレードでは書き換えません。
> `docs/upgrade-guide.md` (配布物では `UPGRADE.md`) の「ディレクトリ一覧を止める」を
> 参照してください。アップグレードツールも、足りていなければ知らせます。
>
> 足した後に 500 になる場合は、サーバの `AllowOverride` が `Options` を
> 許していません。その行を消して `httpd.conf` 側で設定してください。

## v0.3.0 — 2026-08-17

**検索の信頼性を直した版。** 検索インデックスが黙って壊れる経路を塞ぎ、壊れたことに
気付ける手段を用意しました。

> **アップグレードしたら、検索インデックスを 1 回再構築してください。**
> 本文から検索語を切り出すやり方が変わったため、既存のインデックスは古い形式のままです。
> `?option=search_index` の「検索インデックスを再構築」を実行してください。
> 必要かどうかは同じ画面の「詳しく検査する」で確かめられます。

### 修正

- **索引ファイルが読めないときに上書きしなくなった。** 読めなかったものを空とみなして
  書き戻していたため、その状態でページを 1 件保存すると、そのファイルに入っていた
  全ページ分の登録が消えていました。1077 ページの検証で 1 回の保存により 1062 件が
  消え、その語の検索がすべて 0 件になりました。エラーは何も出ません
- **再構築が中断されても、それまでのインデックスが生き残るようにした。** 以前は
  最初に全部消してから作り直していたため、ブラウザを閉じるなどして中断すると
  インデックスが空のまま残り、再構築する前より悪い状態になっていました
- **全角スペースを含むフレーズが検索できるようになった。** 問い合わせ側だけが
  全角スペースを半角に変換していたため、`"設定　方法"` のようなフレーズは本文に
  そのとおり書いてあっても必ず 0 件になっていました
- **`&calendar` などの外部取得に上限時間を設けた。** 応答が無い相手を待ち続け、
  1 ページの表示に 21.76 秒かかる例がありました (`EXTERNAL_FETCH_TIMEOUT`、既定 5 秒)

### 追加

- **`?option=search_index` に検査を載せた。** インデックスとページが一致しているかを
  表示します。「詳しく検査する」でページごとに突き合わせ、登録が欠けているページは
  その場で 1 件ずつ直せます (全体の再構築は不要)
- **`app/tool/search_index_check`** — 同じ検査をコマンドラインから行うツール。
  読み取り専用で、問題があるときだけ終了コード 1 を返すので cron にも置けます。
  1.3.10 より前のサイトにも使えます
- `tests/search-index.sh` — インデックスの整合テスト (37 項目)

### 変更

- **検索結果にページングを入れた** (`SEARCH_RESULTS_PER_PAGE`、既定 50)。
  以前は全件を 1 応答で返していたため、1167 件の検索で 1992KB になっていました
- **1 件あたりのスニペット数に上限を設けた** (`SEARCH_SNIPPET_MAX`、既定 5)。
  上の 2 つで 1992KB → 72KB になります
- 未ログインで検索結果が 0 件のとき、ログインすると範囲が広がる場合がある旨を出すように
  した。件数や有無は出しません (読めないページの存在を漏らさないため)

### 開発

- `tests/env.local.example` に `NF_SITE` と `WIKI_ADMIN` を追加。`WIKI_ADMIN` は
  `setup-fixtures.sh` が以前から参照していたのに書かれておらず、管理者名が `admin`
  以外のサイトでは 1 件もページを書けませんでした

## v0.2.0 — 2026-08-09

**初回公開版。** 機能は v0.1.2 と同じで、公開に向けてドキュメントを整えたものです。

### 追加

- `README.md` を日本語で全面的に書き直した。fork の経緯、v0.1 の実績、
  動作環境、導入手順、ロードマップ
- `docs/installation.md` — 新規インストールの手順書
- `CONTRIBUTING.md` — 開発に参加する人向けの入口
- `CHANGELOG.md` — このファイル
- `tests/env.local` の仕組み。テストの向け先を環境ごとに設定できるようにした

### 変更

- `LICENSE` / `license.txt` を NextForm のステートメントにした。GPLv3 の条文と
  上流の著作権表示はそのまま残し、NextForm の著作権表示と改変告知を加えた
  (GPLv3 §5(a))
- 開発ドキュメントを `docs/development/` にまとめた
- `docs/development/setup.md` を「開発環境の作り方」として書き直した
  (以前は特定環境の構築記録だった)
- ロードマップの版数を 1 つずつ繰り下げた (検索・性能の改善は v0.3 になる)

---

## v0.1.2 — 2026-08-09

アップグレードツールの修正。実運用中の wiki (4,880 ページ / 7.6 GB) への
適用で見つかった問題です。

### 修正

- **バックアップがシンボリックリンクを辿っていた。** 移行してきたサイトに
  リンク切れが残っていると、`copy()` がそこで失敗してバックアップ全体が
  中断していた。リンクは辿らず複製するようにした (`cp -a` と同じ)。
  サイトの外を指すリンクで外の実体まで抱え込む問題も同時に解消した
- バックアップがパーミッション・更新時刻・所有者を引き継いでいなかった
- 失敗したときに、どのパスで何ができなかったのかを表示するようにした

### 変更

- **既定のバックアップを「アップグレードが書き換える範囲 + 設定ファイル」にした。**
  従来はサイト全体を控えていたが、`storage/` が 7.4 GB に対し書き換える範囲は
  2 MB という例があり、割に合わなかった。サイト全体を控えるには `--full-backup`
- バックアップを始める前に空き容量を確認するようにした
- カレンダーのゴールデンマスターを日付非依存にした (テストの修正)

---

## v0.1.1 — 2026-08-03

既存の ToraToraWiki サイトから NextForm へ移る手段を追加しました。

### 追加

- **`app/tool/upgrade`** — アップグレードスクリプト。配布物に同梱される。
  `--dry-run` で何が起きるかを先に確認できる
- **`UPGRADE.md`** — 手作業で行うための手順書。スクリプトの処理と 1 対 1 に対応
- `NEXTFORM_VERSION` を `app/version.inc` に追加。`?option=admin_info` に
  NextForm の版と fork 元の版が並んで表示される

アップグレードは `app/` `resource/` `license.txt` を置き換え、`theme/` を
再生成するだけです。`index.php` `install-info.dat` `storage/` `.htaccess` には
触れません。利用者が `app/plugin/` に置いたプラグインや独自テーマも残します。

---

## v0.1 — 2026-08-03

**PHP 8.3 で警告ゼロで完全に動くこと**をゴールとした最初の版です。
バグ修正と機能追加は意図的に含めていません。

### 修正 — PHP 8 対応

全画面を巡回して出る **4,980 件**の警告・非推奨を 0 件にしました。
変更は 33 ファイル、すべて `app/` の中です。

| 分類 | 件数 | 対応 |
|---|---:|---|
| 参照渡しコールバック (`usort` に `function f(&$a, &$b)`) | 4,554 | 8 関数から `&` を外した |
| null への配列アクセス | 378 | ページツール 11 関数 |
| null 引数の非推奨 (`explode` `unpack` `urlencode`) | 184 | `app/util.inc` の 3 行に集中していた |
| `strftime()` の非推奨 | 37 | `date()` ベースの互換ラッパー `nf_date()` に集約 |
| PHP 8 で削除済みの関数 | 2 | `get_magic_quotes_gpc()` / `each()` |
| 非数値の演算、false への配列アクセス | 4 | |

> 素の ToraToraWiki 1.3.10 は **PHP 8 では起動すらしません**。
> `get_magic_quotes_gpc()` が削除済みで Fatal error になり、
> インストーラにも到達できませんでした。

`strftime()` から `date()` への置き換えでは、単純な読み替えができない指定子が
2 つありました。`%j` (通日) は `date()` の `z` と 1 ずれ、`%W` (週番号) は
`W` (ISO-8601) と定義が異なります。実測して個別に補正しています。

### 修正 — 表示

実運用のテストで見つかったものです。

- インストーラがサイドバーを無効化する設定を書き込み、サイドバーが本文の下に
  回り込んでいた
- 編集画面で、行頭にカーソルがあると Chrome で見えなくなっていた
- `&pre` の長い行が折り返されず、横スクロールが必要だった
  (アスキーアート `&paa` は従来どおり折り返さない)

### 変わっていないもの

**ページの保存形式は上流のままです。** `index.php`、`.htaccess`、`resource/` も
上流と同一です。作業中は一貫してゴールデンマスターテストで HTML 出力を
突き合わせ、振る舞いが変わっていないことを確認しながら進めました。
