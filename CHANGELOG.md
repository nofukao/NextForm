# 変更履歴

このファイルは NextForm の版ごとの変更点を記録します。
fork 元の ToraToraWiki 1.3.10 (rev.1285) からの差分という視点で書いています。

版数は `メジャー.マイナー.パッチ` です。1.0 になるまでは、マイナーの繰り上げで
機能の区切りを、パッチで修正を表します。

---

## v0.2.0 — 2026-08-09

**初回公開版。** 機能は v0.1.2 と同じで、公開に向けてドキュメントを整えたものです。

### 追加

- `README.md` を日本語で全面的に書き直した。fork の経緯、v0.1 の実績、
  動作環境、導入手順、ロードマップ
- `docs/installation.md` — 新規インストールの手順書
- `CONTRIBUTING.md` — 開発に参加する人向けの入口
- `CHANGELOG.md` — このファイル
- `tests/env.local` の仕組み。テストの向け先を環境ごとに設定できるようにした

### 変更

- `LICENSE` / `license.txt` を NextForm のステートメントにした。GPLv3 の条文と
  上流の著作権表示はそのまま残し、NextForm の著作権表示と改変告知を加えた
  (GPLv3 §5(a))
- 開発ドキュメントを `docs/development/` にまとめた
- `docs/development/setup.md` を「開発環境の作り方」として書き直した
  (以前は特定環境の構築記録だった)
- ロードマップの版数を 1 つずつ繰り下げた (検索・性能の改善は v0.3 になる)

---

## v0.1.2 — 2026-08-09

アップグレードツールの修正。実運用中の wiki (4,880 ページ / 7.6 GB) への
適用で見つかった問題です。

### 修正

- **バックアップがシンボリックリンクを辿っていた。** 移行してきたサイトに
  リンク切れが残っていると、`copy()` がそこで失敗してバックアップ全体が
  中断していた。リンクは辿らず複製するようにした (`cp -a` と同じ)。
  サイトの外を指すリンクで外の実体まで抱え込む問題も同時に解消した
- バックアップがパーミッション・更新時刻・所有者を引き継いでいなかった
- 失敗したときに、どのパスで何ができなかったのかを表示するようにした

### 変更

- **既定のバックアップを「アップグレードが書き換える範囲 + 設定ファイル」にした。**
  従来はサイト全体を控えていたが、`storage/` が 7.4 GB に対し書き換える範囲は
  2 MB という例があり、割に合わなかった。サイト全体を控えるには `--full-backup`
- バックアップを始める前に空き容量を確認するようにした
- カレンダーのゴールデンマスターを日付非依存にした (テストの修正)

---

## v0.1.1 — 2026-08-03

既存の ToraToraWiki サイトから NextForm へ移る手段を追加しました。

### 追加

- **`app/tool/upgrade`** — アップグレードスクリプト。配布物に同梱される。
  `--dry-run` で何が起きるかを先に確認できる
- **`UPGRADE.md`** — 手作業で行うための手順書。スクリプトの処理と 1 対 1 に対応
- `NEXTFORM_VERSION` を `app/version.inc` に追加。`?option=admin_info` に
  NextForm の版と fork 元の版が並んで表示される

アップグレードは `app/` `resource/` `license.txt` を置き換え、`theme/` を
再生成するだけです。`index.php` `install-info.dat` `storage/` `.htaccess` には
触れません。利用者が `app/plugin/` に置いたプラグインや独自テーマも残します。

---

## v0.1 — 2026-08-03

**PHP 8.3 で警告ゼロで完全に動くこと**をゴールとした最初の版です。
バグ修正と機能追加は意図的に含めていません。

### 修正 — PHP 8 対応

全画面を巡回して出る **4,980 件**の警告・非推奨を 0 件にしました。
変更は 33 ファイル、すべて `app/` の中です。

| 分類 | 件数 | 対応 |
|---|---:|---|
| 参照渡しコールバック (`usort` に `function f(&$a, &$b)`) | 4,554 | 8 関数から `&` を外した |
| null への配列アクセス | 378 | ページツール 11 関数 |
| null 引数の非推奨 (`explode` `unpack` `urlencode`) | 184 | `app/util.inc` の 3 行に集中していた |
| `strftime()` の非推奨 | 37 | `date()` ベースの互換ラッパー `nf_date()` に集約 |
| PHP 8 で削除済みの関数 | 2 | `get_magic_quotes_gpc()` / `each()` |
| 非数値の演算、false への配列アクセス | 4 | |

> 素の ToraToraWiki 1.3.10 は **PHP 8 では起動すらしません**。
> `get_magic_quotes_gpc()` が削除済みで Fatal error になり、
> インストーラにも到達できませんでした。

`strftime()` から `date()` への置き換えでは、単純な読み替えができない指定子が
2 つありました。`%j` (通日) は `date()` の `z` と 1 ずれ、`%W` (週番号) は
`W` (ISO-8601) と定義が異なります。実測して個別に補正しています。

### 修正 — 表示

実運用のテストで見つかったものです。

- インストーラがサイドバーを無効化する設定を書き込み、サイドバーが本文の下に
  回り込んでいた
- 編集画面で、行頭にカーソルがあると Chrome で見えなくなっていた
- `&pre` の長い行が折り返されず、横スクロールが必要だった
  (アスキーアート `&paa` は従来どおり折り返さない)

### 変わっていないもの

**ページの保存形式は上流のままです。** `index.php`、`.htaccess`、`resource/` も
上流と同一です。作業中は一貫してゴールデンマスターテストで HTML 出力を
突き合わせ、振る舞いが変わっていないことを確認しながら進めました。
