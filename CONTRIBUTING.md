# NextForm の開発について

NextForm は ToraToraWiki 1.3.10 の fork です。
**約 21,800 行 / 212 ファイルの、型宣言のない手続き型 PHP** を相手にしています。
テストが後付けであることも含めて、進め方には少し工夫が要ります。

このファイルはその要約です。詳細は次を参照してください。

| | |
|---|---|
| [docs/development/project-overview.md](docs/development/project-overview.md) | 目的、上流の実構造、確定した設計判断、ロードマップ |
| [docs/development/workflow.md](docs/development/workflow.md) | 開発フローとテスト戦略の詳細 |
| [docs/development/setup.md](docs/development/setup.md) | 開発環境の作り方 |

---

## まず守ってほしいこと

**リポジトリに資格情報と環境固有の値を書かないでください。**
パスワード、認証ダイジェスト、内部のホスト名や IP は、コードにも
ドキュメントにもコミットメッセージにも入れないでください。
一度コミットすると履歴から消すのが面倒です。

テストの向け先などの環境依存の値は `tests/env.local` に書きます。
このファイルは `.gitignore` されています。
雛形は [tests/env.local.example](tests/env.local.example) にあります。

---

## 開発の流れ

```
① 調査      コードを読み、現状と影響範囲を報告する
② 設計メモ   やること・やらないこと・想定リスクを短く書き出す ← ここが唯一のゲート
③ ブランチ   作業単位でブランチを切る
④ テスト先行 「変更前の振る舞い」を先に固定する
⑤ 実装      最小の差分で変更する
⑥ 検証      テスト再実行 + エラーログの確認 + ブラウザで実際に触る
⑦ レビュー
⑧ マージ
```

### ④ を飛ばさないでください

型宣言が無く、テストも後付けの 21,800 行に対して、
**変更前の出力を固定せずに手を入れるのは危険**です。
v0.1 では 33 ファイルを書き換えましたが、ゴールデンマスターが一貫して
一致し続けたことで「振る舞いは変えていない」と言い切れました。

### 作業中に別の問題を見つけたら

**直さずに報告してください。** 直すかどうかは別に判断します。
1 つのブランチに複数の意図が混ざると、あとから差分が読めなくなります。

---

## テスト

4 本立てです。それぞれ守っている範囲が違います。

```bash
./tests/setup-fixtures.sh   # 入力ページを投入 (冪等)
./tests/golden.sh           # HTML 出力が変わっていないこと
./tests/smoke.sh            # 全 42 画面の巡回 + PHP 警告の集計
./tests/css-rules.sh        # 生成された静的 CSS のルール検査
./tests/upgrade.sh          # 既存サイトの複製に対してアップグレードを実走
```

- **ゴールデンマスター** はパーサの出力だけを対象にします。`?option=allpage` の
  ような内容依存の一覧を混ぜると、ページを 1 つ足しただけで落ちてしまい、
  「振る舞いが変わったのか、データが変わっただけなのか」が区別できなくなります
- **css-rules.sh** があるのは、ゴールデンマスターが HTML しか見ないからです。
  生成 CSS の崩れを 2 件取り逃がした実績があります。CSS 由来の不具合を直したら、
  ここに 1 行足してください
- **upgrade.sh** は手順書 (`docs/upgrade-guide.md`) のコマンドを**そのまま実行**します。
  ドキュメントは放っておくと実物とずれるためです。手順書を変えたらテストも
  合わせてください

テストは日付や実行時刻に依存させないでください。
採取した日にしか通らないテストになります (実際に一度そうなりました)。

---

## コーディング

- **既存のスタイルに合わせてください。** タブインデント、命名、構造。
  PSR-12 への一括整形はしないでください。差分が読めなくなります
- **ランタイム依存を増やさないでください。** 「tar.gz を展開するだけで動く」
  という上流の美点を守ります。composer は開発用 (`require-dev`) のみで、
  `vendor/` は配布物に含めません
- ライセンスは **GPLv3** です (上流から継承)

### 日付の扱い

`strftime()` は使わないでください。`app/util.inc` の **`nf_date()`** に集約してあります。
`TIME_FORMAT` / `DATE_FORMAT` は管理画面から変更できる strftime 形式の文字列なので、
`%-m` のような glibc 拡張にも対応しています。

`date()` は `strftime()` の素直な読み替えになりません。実測で `%j` は `z` と
1 ずれ、`%W` は `W` (ISO-8601) と定義が違います。

### テーマ

`app/theme/` は**ソース**で、`theme/` が生成物です。
ソースを変えたら静的 CSS の再生成が必要です。忘れるとブラウザに反映されません。

---

## Git

- `main` に直接コミットしないでください。作業単位でブランチを切ります
- プレフィックス: `feat/` `fix/` `test/` `docs/` `chore/` `refactor/`
- **1 つの意図 = 1 コミット。** 機械的な一括置換と手作業の判断は分けてください
- コミットメッセージの 1 行目は命令形の要約 (72 文字以内)。
  本文には**なぜそうしたのか**を書いてください。何をしたかは差分を見れば分かります

---

## 上流との関係

上流は 2015 年以降更新されていないため、**独立した fork** として進めています。
変更を送り返すことはしていません。

素の上流はタグ `upstream/1.3.10` にあります。判断に迷ったら比較できます。

```bash
git diff upstream/1.3.10 HEAD -- NextForm/app/
```

上流のファイルを削除・改名するときは慎重に。
アップグレードツールが「配布物に無いファイルは削除しない」方針で動いており、
利用者のサイトには上流のファイルがそのまま残っています。
